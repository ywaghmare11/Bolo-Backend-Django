Django==6.0.8
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
psycopg[binary]==3.3.4
django-environ==0.14.0
# redis 8.x defaults its connection protocol to RESP3 (sends a HELLO handshake), which
# only Redis 6+ speaks -- pinned to 5.2.1 here since it's RESP2-only, no HELLO handshake,
# and celery/kombu's redis transport has no way to pass a protocol override through its
# own URL parsing (django-redis does, kombu doesn't). RESP2 is a strict subset every
# Redis version speaks, so this is a safe pin even against a newer Redis in prod.
redis==5.2.1
django-redis==7.0.0
celery==5.6.3
# S3-compatible object storage for evidence files. IAM-role-only via the default
# credential provider chain in prod (no AWS_ACCESS_KEY_ID/SECRET_ACCESS_KEY env vars),
# same pattern as SES -- see docs/api/api-spec.md §5 and CLAUDE.md's env var table.
boto3==1.40.76
# Server-side re-sanitization of broadcast messageHtml (TipTap-authored rich text) --
# guidelines.md's Security section names bleach explicitly, unlike upstream's own
# hand-rolled safelist sanitizer (utils/htmlSanitize.ts) -- this project's own stated
# tool choice takes precedence over the Node implementation detail.
bleach==6.4.0
# Global Search's query-understanding layer (docs/api/global-search-ai-contract.md) --
# typo correction, roster-grounded person-name resolution, status/priority/due filter
# extraction. Pinned to the stable 1.x client API (OpenAI(...).chat.completions.create),
# not the newer 2.x/3.x major lines, since that's the interface this module is written
# against. No OPENAI_API_KEY is configured in this dev sandbox, so search runs the
# documented AI-unavailable fallback path here -- same "no real credentials, mock/skip
# the external call" situation as boto3/S3 in apps/evidence and apps/broadcasts.
openai==1.109.1
# CORS for local cross-origin dev only -- bolo-web (vite, localhost:5173) calling this
# API directly (localhost:8000) instead of through a same-origin nginx proxy like prod.
# Only enabled in config/settings/dev.py, never base.py/prod.py.
django-cors-headers==4.9.0
# Structured JSON logging with request-id/tenant-id/actor-id correlation
# (ROADMAP.md Phase 10, guidelines.md's Logging section) -- config/settings/base.py.
structlog==26.1.0
# OpenAPI 3 schema generated from the DRF views + a Swagger UI / ReDoc browser
# (ROADMAP.md Phase 13). The -sidecar package vendors the Swagger/ReDoc JS+CSS as
# static files so the docs pages don't depend on an external CDN at runtime --
# served by WhiteNoise in the container (see requirements/prod.txt).
drf-spectacular==0.28.0
drf-spectacular-sidecar==2025.7.1
# Multi-format member bulk-import ETL for PlatformAdmin (ROADMAP.md Phase 15c).
# pandas is the tabular Transform layer (vectorized header normalization, type
# coercion, validation, within-file dedup, formula-injection neutralization);
# openpyxl is the engine pandas.read_excel() needs to parse .xlsx (the old xlrd
# engine dropped .xlsx support in 2020). numpy rides in as a pandas dependency --
# not pinned separately, since pandas already constrains a compatible version.
# Both are imported lazily inside apps/platform_admin/etl.py (only touched on an
# actual import request), same pattern as boto3/openai elsewhere.
pandas==2.3.3
openpyxl==3.1.5
